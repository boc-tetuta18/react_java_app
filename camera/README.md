
  # Camera Control Screen Design

  This is a code bundle for Camera Control Screen Design. The original project is available at https://www.figma.com/design/jgrGQOnqWp7OOaNrckRUNZ/Camera-Control-Screen-Design.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  